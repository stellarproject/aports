---
kind: pipeline
type: exec
name: aports

platform:
  os: linux

steps:
- name: build
  commands:
    - chmod -R +rw .
    - sudo -u $USER /bin/sh -c "make PACKAGE_DIR=$PACKAGE_DIR terra"
  environment:
    PACKAGE_DIR:
      from_secret: package_dir
    USER:
      from_secret: user

node:
  type: alpine-builder
